# publish-lambda-layer

Publish a Lambda Layer ZIP to GitHub Releases on every release; additionally deploy that layer to all commercial AWS regions with public read access via OIDC-federated GitHub Actions
